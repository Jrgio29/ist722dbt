verison: 2

models:
  - name: dim_date
    description: Confromed Data Dimension. One row per day of the yeat.
    columns:
      - name: datekey
        data data_type: int
        description: The surrogae key of the date YYYMMDD
        tests:
          - not_null
          - unique
      - name: date
        description: The date of the date key
        tests:
          - not_null
          - unique
          